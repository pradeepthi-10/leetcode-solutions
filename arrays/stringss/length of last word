class Solution {
    public int lengthOfLastWord(String s) {
     

        String[] sp = s.split(" ");

        String word = sp[sp.length - 1];

        return word.length();
  
    }
}
//we are splitting the input string s into an array of words using the split method with a space as the delimiter. Then, we