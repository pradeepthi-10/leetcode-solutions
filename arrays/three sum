class Solution {
    public List<List<Integer>> threeSum(int[] nums) {
     Arrays.sort(nums);
        List<List<Integer>> result = new ArrayList<>();
        int comp = 0;
        for(int i=0 ; i < nums.length ; i++){
            int first = nums[i];
            if (i > 0 && nums[i] == nums[i - 1]) 
            {
                 continue;
                  }
         HashMap<Integer,Integer> map = new HashMap<>();
        for(int j=i+1 ; j < nums.length ; j++){ 
            comp = 0 - first - nums[j];
              if(map.containsKey(comp)){
                result.add(Arrays.asList(first, nums[j], comp));
              }
              
              map.put(nums[j],j);
        }
        }
        return result;
    }
}
//in this problem we are trying to find all unique triplets in the array which gives the sum of zero. We first sort the array and then iterate through the array. For each element, we use a hash map to find if there exists a pair of numbers that sum up to the negative of the current element. If such a pair is found, we add the triplet to the result 
//if the current element is the same as the previous element, we skip it to avoid duplicates. Finally, we return the list of unique triplets. This approach has a time complexity 