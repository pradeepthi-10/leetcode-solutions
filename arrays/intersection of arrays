class Solution {
    public int[] intersection(int[] nums1, int[] nums2) {
        ArrayList<Integer> list = new ArrayList<>();
        Arrays.sort(nums1);
        Arrays.sort(nums2);
        for(int i=0;i<nums1.length;i++){
            for(int j=0;j<nums2.length;j++){
                if(nums1[i]==nums2[j]){
                    if(!list.contains(nums1[i])){
                        list.add(nums1[i]);
                        break;
                    }

                }
            }
        }
        int[] ans = new int[list.size()];
        for(int i =0;i<list.size();i++){
            ans[i]=list.get(i);
        }
        return ans;
    }
}
// this problem can also be done in other way without using list we just have to take the ans array of size min(nums1.length,nums2.length)
//we just used arraylist because the size of new array is dynamic we dont exactly know 