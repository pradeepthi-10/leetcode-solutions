class Solution {
    public int pivotIndex(int[] nums) {
        int totalsum=0;
        for(int i=0;i<nums.length;i++){
            totalsum+=nums[i];
        }
        int ls=0;
        for(int i=0;i<nums.length;i++)
        {
            int rs= totalsum-ls-arr[i];
            if(ls==rs)
                return i;
            else
                ls+=nums[i];
        }
        return -1;
    }
}
