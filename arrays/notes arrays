question : arrays 
283.move zeros
=>using two pointers
🔹 p1 → traversal pointer
🔹 p2 → position to place next non-zero
in this problem 2 pointers means two variables p1 and p2
first we should understand that p1 is used for traversal
of the array 
p2 we have to assume that it is placed at the 0
we dont need to check if p2 is non zero and increment
directly we have to assume 
then check if p1 not equals 0
we have to swap with p1 with p2 and increment p2
at last print it
remember p2 is not at 0 place it is at a place where the 
next non zero element should be replaced

//question 4
question : arrays 
26.remove duplicates from sorted array 
=>using two pointers
p2:is used for traversing
p1:points to the last unique element
If nums[p2] is different from nums[p1], we've found a new unique value:
=>Move p1 one step.
=>Copy nums[p2] to nums[p1].
eg:[1,1,2]
now p1 is at index0 p2 is at 1 checks p1!=p2 then
p1 moves p1++ then p2 copies value into p1 thats it
we want count so we are printing p1+1

//question 5
27.remove element
=>using two pointers
here the main logic in this question is first p1 is from the begining
and p2 is from ending now we should move p1 when p1 is not equal to the value becoz
if it is equal to the value we should swap
and the thing is we should not restrict p2 like p2!=val this is not needed
even if it is equal or not equal from the end we should swap and decrement 
eg:Example:

[3, 2, 3]
 ↑     ↑
p1    p2
Copy 3 to p1:
[3, 2, 3]
Nothing changes.
Then p2--.